services:
  - type: web
    name: startup-survey
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn app:app
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: survey-db
          property: connectionString

databases:
  - name: survey-db
    plan: free
